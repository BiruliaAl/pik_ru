let swiper = new Swiper('.apartments-slider', {
    // Optional parameters
    loop: true,
    autoHeight: false,
  
    // If we need pagination
    pagination: {
      el: '.swiper-pagination',
      clickable: true,
    },

  
  });
